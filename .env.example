# Arquivo de exemplo para variáveis de ambiente.
# Copie este arquivo para .env e preencha com suas próprias chaves.
# NUNCA comite o arquivo .env para o repositório.

# --- Credenciais do Firebase (Client-side) ---
# Encontradas em: Configurações do Projeto > Geral > Seus apps > App da Web
NEXT_PUBLIC_FIREBASE_PROJECT_ID=SEU_ID_DO_PROJETO
NEXT_PUBLIC_FIREBASE_APP_ID=SEU_APP_ID
NEXT_PUBLIC_FIREBASE_API_KEY=SUA_API_KEY_DA_WEB
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=SEU_DOMINIO_DE_AUTENTICACAO
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=SEU_ID_DO_REMETENTE

# --- Chave da API da OpenAI (Server-side) ---
# Encontrada no painel da sua conta OpenAI: https://platform.openai.com/api-keys
OPENAI_API_KEY=sua_chave_secreta_sk-...

# --- Chave de Conta de Serviço do Firebase Admin (Server-side) ---
# Encontrada em: Configurações do Projeto > Contas de serviço > Gerar nova chave privada
# Copie o CONTEÚDO COMPLETO do arquivo JSON e cole aqui, geralmente entre aspas.
FIREBASE_SERVICE_ACCOUNT_KEY='{"type": "service_account", "project_id": "...", ...}'
