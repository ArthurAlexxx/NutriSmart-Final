rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regras de desenvolvimento: Permite leitura e escrita para qualquer usuário autenticado.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
